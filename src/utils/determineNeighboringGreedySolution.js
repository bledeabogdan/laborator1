
let allNeighboringSolutions = [];

const determineAllNeighbors = (solution, indexArray) =>{

}
const determineNeighboringGreedySolution = (objectSolution, indexArray, objects, maxWeight) => {
    determineAllNeighbors(objectSolution.solution, indexArray);
}

export default determineNeighboringGreedySolution;